{
  "name": "ecommerce-checkout-payment-system",
  "version": "1.0.0",
  "private": true,
  "description": "MERN E-Commerce Checkout & Mock Payment System — monorepo root scripts",
  "scripts": {
    "install:all": "cd Backend && npm install && cd .. && cd Frontend && npm install",
    "server": "cd Backend && npm run dev",
    "client": "cd Frontend && npm start",
    "dev": "concurrently -n SERVER,CLIENT -c blue,green \"npm run server\" \"npm run client\""
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
